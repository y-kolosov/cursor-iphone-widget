# Small widget layout — Cursor Usage

Дата: 2026-09-12  
Статус: draft, ждёт ревью  
Дополняет: [2026-09-11-cursor-usage-iphone-widget-design.md](./2026-09-11-cursor-usage-iphone-widget-design.md)

## Проблема

Small сейчас — урезанная копия medium. Заголовок `Cursor` дублирует подпись первого бара и вместе с ней режется (`Curserror` / `Cothers`). Дата сброса занимает футер. On-demand на small нет — хотя это как раз та сумма, ради которой смотрят виджет, когда Other Models уже 100%.

Модель данных та же; ломается только иерархия на маленькой карточке.

## Цель

Small с первого взгляда отвечает: сколько съедено по двум пулам и, если есть, сколько on-demand. Дата сброса остаётся запасным футером, когда перерасхода нет.

## Вне скоупа

- Medium, парсер, токен, кэш, refresh, Shortcut
- Новые поля (used / limit / remaining, время съёмки на small)
- Другая композиция (крупные цифры без баров, смена подписей `Cursor` / `Others`)
- Lock Screen / WidgetKit

## Подход

Вариант A: та же стековая сетка, другой смысл хрома. Заголовок больше не слово «Cursor». Футер — одна строка: on-demand **или** дата, не обе.

## Вёрстка small

Сверху вниз, одна колонка:

1. Заголовок `Usage` — тот же мелкий приглушённый стиль, что был у `Cursor`.
2. Бар `Cursor` + целое `%` (или `∞` при `unlimited`).
3. Бар `Others` + целое `%` (или `∞`).
4. Одна строка футера:
   - `onDemandUsd != null` → `On-demand $X.XX` (`toFixed(2)`), фиолетовый, как на medium;
   - иначе, если есть `resetAt` → `d MMM` (локаль устройства), приглушённый;
   - иначе пусто, без `$0.00` и без слова `reset`.

Времени съёмки на small нет.

Подписи баров на small остаются `Cursor` и `Others`. Полные `Cursor Models` / `Other Models` — только medium.

## Обрезка

Заголовок и подписи баров: `lineLimit = 1`. На экране должно читаться ровно `Usage`, `Cursor`, `Others` — без склейки и обрезки. Строка «подпись + %» умещается в одну линию. Если ширина дорожки бара задана числом, оно не больше полезной ширины карточки (текущие 128 pt на small как раз выталкивают текст).

Цвета пулов, клип заливки на 100%, цифра справа с фактическим процентом (в т.ч. > 100) — как в спеке 2026-09-11.

## Данные

Та же `UsageModel`. Правило on-demand не меняется: поле не `null` только если `enabled === true` и `used > 0`. Small и medium читают одно поле; разница только в том, что small в футере **заменяет** дату on-demand, а medium показывает дату в шапке и on-demand снизу независимо.

## Ошибки и stale

Без модели — как сейчас, одно сообщение, без баров: `Add token`, `Token expired`, `Can't parse`, `Offline`.

Stale (кэш): проценты приглушённые. Футер тот же (on-demand или дата). On-demand остаётся фиолетовым, дата — приглушённой. Время съёмки на small не появляется.

Unlimited: `∞` вместо `%`, футер по тем же правилам.

## Компоненты

| Путь | Изменение |
|---|---|
| `scriptable/CursorUsage.js` | `usageWidget` для `family === "small"`: заголовок, футер, ширина бара / `lineLimit` |
| `README.md` / `README.ru.md` | Чеклист small: on-demand **или** дата, нет обрезки |
| `lib/parse-usage.js`, хелпер, тесты парсера | Не менять |

## Тестирование

Автотесты парсера не трогаем — модель та же.

Ручной чеклист:

- [ ] Small с on-demand > 0: `Usage`, `Cursor`, `Others`, футер `On-demand $X.XX`, даты нет
- [ ] Small без on-demand: в футере дата `d MMM`
- [ ] Small, нет `resetAt` и нет on-demand: футер пустой
- [ ] Подписи не обрезаны и не склеены
- [ ] Medium без изменений: шапка `reset … · HH:mm`, снизу on-demand если есть
- [ ] Светлая и тёмная тема
- [ ] Stale: серые %, футер на месте
- [ ] Ошибки без модели — одно сообщение

## Критерий готовности

- Small показывает оба процента и on-demand, когда он есть.
- Small без on-demand показывает дату сброса, не пустой футер (если дата есть).
- Заголовок `Usage`, подписи `Cursor` / `Others` читаются целиком.
- Medium, парсер и токен ведут себя как раньше.
